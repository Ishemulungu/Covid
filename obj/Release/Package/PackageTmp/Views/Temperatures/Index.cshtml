@model IEnumerable<Covid.Models.Temperature>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Temperature1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.Full_Names)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Temperature1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.Full_Names)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Temp_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Temp_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Temp_Id })
        </td>
    </tr>
}

</table>
